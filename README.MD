### Shared package usage

1. Add inside your repository action secret with name BRO_NPM_TOKEN
2. Place there value with packages read permission (read:packages)
3. Check auto-approver repo as an example
